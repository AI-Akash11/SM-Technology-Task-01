const SectionContainer = ({children}) => {
    return (
        <div className="mx-[15px] md:mx-4 xl:max-w-[1320px] xl:mx-auto">
            {children}
        </div>
    );
};

export default SectionContainer;